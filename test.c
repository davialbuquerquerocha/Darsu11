#include <stdio.h>
#include <math.h>

int main (){
    double a, b, c, delta;

    printf("a: "); scanf("%lf", &a);
    printf("b: "); scanf("%lf", &b);
    printf("c: "); scnaf("%lf", &c);

    if (delta < 0)